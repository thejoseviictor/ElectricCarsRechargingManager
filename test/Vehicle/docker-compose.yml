version: "3.8"

services:
  vehicle:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: vehicle
    networks:
      - recharging_manager

networks:
  recharging_manager:
    external: true
